{
  "agentCanUpdateSnapshot": true,
  "install": "python3 -m venv venv && . venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt && playwright install",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "terminals": [
    {
      "name": "Run Tests",
      "command": ". venv/bin/activate && pytest"
    }
  ]
}
