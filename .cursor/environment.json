{
  "agentCanUpdateSnapshot": true,
  "install": "sudo apt update && sudo apt install -y python3.13-venv && python3 -m venv venv && . venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt",
  "terminals": [
    {
      "name": "Run Tests",
      "command": ". venv/bin/activate && pytest"
    }
  ]
}
